# admin_service/urls.py
from django.urls import path
from . import views

urlpatterns = [
    # Định nghĩa các đường dẫn URL của bạn ở đây, ví dụ:
    # path('some-url/', views.some_view, name='some_view'),
]
